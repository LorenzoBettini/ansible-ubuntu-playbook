- name: Install Gnome Packages
  become: yes
  apt:
    state: latest
    pkg:
    - gnome-tweaks
    - guake
  register: aptout

- debug: msg="{{ aptout.stdout_lines }}"
  when: aptout.stdout_lines is defined
